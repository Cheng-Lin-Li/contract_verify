# === contract_verify — 3-month backend dependencies =========================
# These power the skeleton modules in app/api, app/core/security, app/models,
# app/knowledge, app/queue, app/services, app/storage/postgres and the PaddleOCR
# engine. They are NOT needed for the MVP CLI or the offline demo/tests.
# Install alongside backend/requirements.txt when implementing the 3-month scope.

# --- API service ---
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9        # file uploads

# --- Auth / RBAC ---
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7

# --- Persistence (Postgres) ---
SQLAlchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13

# --- Vector retrieval ---
qdrant-client>=1.9
# Local embeddings runtime is provided via Ollama (bge-m3); sentence-transformers
# is optional if you prefer in-process embedding:
# sentence-transformers>=3.0

# --- OCR (tables & images) ---
paddleocr>=2.7
paddlepaddle>=2.6

# --- Object storage (already in requirements.txt as optional) ---
boto3>=1.34

# --- Tests for the 3-month specs ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27                    # FastAPI TestClient transport
